version: "2"
settings:
  conductor_base: ubuntu:16.04

services:
   www:
     from: ubuntu:16.04
     container_name: www
     roles:
       - softasap.sa-nginx-container
     volumes:
         - "$PWD/app/www:/static"
