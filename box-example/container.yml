version: "2"
settings:
  conductor_base: ubuntu:16.04

services:
   nginx:
     from: ubuntu:16.04
     container_name: sa-nginx-container
     roles:
       - softasap.sa-nginx-container
     volumes:
         - "$PWD/app/www:/static"
